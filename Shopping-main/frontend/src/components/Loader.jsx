// src/components/Loader.jsx

// Used while waiting for data (e.g. products from the API)
const Loader = () => (
  <p style={{ textAlign: "center" }}>⏳ Loading products...</p>
);

export default Loader;
